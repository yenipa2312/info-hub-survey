# Copy this file to .env for local testing (netlify dev reads it automatically).
# Not actually needed by this project yet (no Claude API calls here), kept
# for consistency with the sibling feedback-triage-tool project and in case
# an "analyze open text" feature gets added later.
ANTHROPIC_API_KEY=your-key-here
